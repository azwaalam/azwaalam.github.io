// Project Title
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"


// (x,y) > x1 = ax + by + s
//         y1 = cx + dy + t 
let newX, newY;
let a, b, c, d, s, t;

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}

function flame(x, y){
  line()
}